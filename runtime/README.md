# runtime

このディレクトリは、`地層` に上げるほどではない短命の runtime 補助を置く場所です。

## 典型例

- `*_CURRENT_AIR.md`
  - companion の `今日の空気` を 1〜3 行だけ持つ flash memory

## ルール

- `*_CURRENT_AIR.md` のような実体は git に積まない
- 消えても困らないものだけ置く
- 長く残したいものは `memory` や `docs/worklog` 側へ昇格させる
